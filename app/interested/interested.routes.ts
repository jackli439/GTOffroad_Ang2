import { Routes } from '@angular/router';

import { InterestedComponent } from './interested.component';

// Route Configuration
export const interestedRoutes: Routes = [
  { path: 'interested', component: InterestedComponent },
];